spring.application.name=Spring-backend

# Server Configuration
server.port=8080

# File Upload Settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Firebase Configuration
firebase.service-account-key=${FIREBASE_CREDENTIALS_PATH:classpath:serviceAccountKey.json}
firebase.storage-bucket=${FIREBASE_STORAGE_BUCKET:smart-study-buddy-c59ca.firebasestorage.app}